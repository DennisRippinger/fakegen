package de.drippinger.fakegen;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * @author Dennis Rippinger
 */
public class TestDataFillerRuleTest {

    @Test
    @DisplayName("Should write which seed was used for failing test")
    void should_write_which_seed_was_used_for_failing_test() {
        // The Test Utils to test Junit with Junit are to be published in version 1.4, which is currently only available
        // as snapshot.
    }
}
